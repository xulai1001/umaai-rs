[workspace]
members = ["crates/umasim", "crates/umaai"]
resolver = "2"

# 性能档（2026-08-27，本 workbench 决策）：原为 opt-level='z'+lto=false 的体积最小档，
# 对批量育成模拟是 2~4× 的白给减速。改为速度档三件套。
# 可复现性说明：rustc 不做浮点重结合（无 fast-math），opt/lto/codegen-units 不改变
# 任何整数/浮点求值结果——配对闸门的位级一致性不受影响；CI 中 candidate 与
# 上游基线在同一工作区配置下分别重编，口径天然对齐。
[profile.release]
opt-level = 3
debug = true
codegen-units = 1
lto = "thin"

[workspace.dependencies]
umasim = { path = "crates/umasim" }
umaai = { path = "crates/umaai" }
anyhow = "1.0.100"
bincode = "1.3.3"
chrono = "0.4.42"
clap = { version = "4.5.50", features = ["derive"] }
colored = "3.0.0"
csv = "1.4.0"
lexopt = "0.3.2"
comfy-table = { version = "7.2.1", features = ["custom_styling"] }
enum-iterator = "2.3.0"
env_logger = "0.11.8"
fs-err = "3.1.3"
hashbrown = { version = "0.16.0", features = ["serde"] }
indicatif = "0.18.1"
log = "0.4.28"
notify = "8.2.0"
rand = { version = "0.9.2", features = ["serde", "std_rng"] }
rand_distr = "0.5.1"
rayon = "1.11.0"
#salvo = "0.84.2"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
thiserror = "2.0.17"
tokio = { version = "1.48.0", features = ["full"] }
toml = { version = "0.9.8", features = ["preserve_order"] }
inquire = "0.9.1"
ratatui = { version = "0.29.0", features = ["all-widgets", "macros", "palette", "serde"] }
text-to-ascii-art = "0.1.10"
flexi_logger = "0.31.7"

# ONNX 推理
tract-onnx = "0.22.0"
ndarray = "0.17.1"

# 保序 map（bench player_builds 解析；已由 toml 间接依赖，仅提升为直接依赖）
indexmap = { version = "2.14.0", features = ["serde"] }

# CPU/Heap profiler（pprof-rs，Google pprof 协议；sim_profiler bin 用）
pprof = "0.15.0"

# Git 钩子分发（cargo-husky：构建时把 .cargo-husky/hooks/ 复制到 .git/hooks/）
# cargo-husky = { version = "1.5.0", default-features = false, features = ["user-hooks"] }
